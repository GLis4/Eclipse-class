package desafios;

import java.util.ArrayList;
import java.util.List;
import java.util.Random;
import java.util.stream.Stream;

public class SorteioGrupo {

	public static void main(String[] args) {


		String nomes = "Ana,joao,maria,linda,senhora,madame";
		
		
		
		@SuppressWarnings("unchecked")
		List<String> nomesAlunos = (List<String>) Stream.of(nomes.split(","));
		
		System.out.println(nomesAlunos.get(1));
		
		/*Random rm = new Random(nomesAlunos.size());


		for(int i = 0 ; i < 6; i++) {

			int select = rm.nextInt( nomesAlunos.size() + 1);

			if(select + 1  < nomesAlunos.size()) {

				System.out.println("Grupo #" + i);

				System.out.println(nomesAlunos.get(select));
				System.out.println(nomesAlunos.get(select + 1 ));

				nomesAlunos.remove(select + 1);
				nomesAlunos.remove(select);
				
			}

			System.out.println("");
		}*/
	}
}
